add a server:

    blinder un peu le serveur : 
        empêcher d'insérer des caractères spéciaux dans le nom ... seulement [a-zA-Z0-9 -_]*
        gérer l'interdiction côté client
        regarder ce que c'est le protect forgery ...
        
    style the high scores (even and odd)
    GPL the code
    add a rules tab
    add a credit tab with links to the code

add iphone compatible buttons